# learnings epoch-003 v3.1.0 | 2026-03-21T20:30Z | nextID:L119
# carried forward from epoch-002 (L001-L023) + epoch-003 additions
# format: ENCODING.md v1.0 compact (primary). JSON audit at .audit suffix
# NOTE: L025-L048 exist on IGS only (not yet synced). L078-L082 gap (session files, not consolidated).

L001:to:c:orientation-survival-001
MCP Shell swallows git stdout
Use udc:execute_command not Windows-MCP:Shell for git.

L002:to:c:orientation-survival-001
MCP Shell PATH resets every command
Each invocation fresh. Use full paths or udc:execute_command.

L003:to:c:orientation-survival-001+opus46-coldstart-recon-001
MCP Shell is PowerShell only AND fully broken on BIGSPIDER
FileNotFoundError on ALL commands. Use udc:execute_command + "C:\Program Files\PowerShell\7\pwsh.exe" for PS needs.

L004:or:c:orientation-survival-001
Read todo.json first not all handovers
buffer=context. todo.json=queue. handovers=deep history only.

L005:or:cf:multiple
Orientation kills sessions — be surgical
Read startup config,buffer,todo,learnings. Then ask user. Don't explore.

L006:gt:c:orientation-survival-001
SSH may fail — HTTPS fallback
git remote set-url origin https://github.com/kawaisunn/REPO.git

L007:pj:c:orientation-survival-001
Two keywards implementations unreconciled
C:\dev\keywrd\(standalone,nltk/sklearn) vs C:\dev\ocritd\engine\keywards\(integrated,freq/TF-IDF). User decides.

L008:bd:c:multiple
System Python != embedded Python
System:3.14+3.11. Embedded:3.13.11@C:\dev\ocritd\runtime\python\. ALWAYS use embedded. NEVER touch sys/user PATH.

L009:or:!c:cf:ocritd-stabilize-001+opus46-coldstart-recon-001
CRITICAL: bash_tool is Linux — check UDC/Windows-MCP first
bash reports Linux even when Windows MCP available. tool_search BEFORE bash. Every session that skips this makes the mistake. Confirmed across Sonnet+Opus.

L010:or:cf:ocritd-stabilize-001
Never respond before verification completes
No greeting,no status,no claims until claude_start.json read+tests pass. Breaks trust.

L011:or:c:phantom-0216
GitHub file reads are expensive fallbacks
Local filesystem via UDC >> GitHub API. GitHub for push/commit only.

L012:si:!c:cl:ocritd-stabilize-002
CRITICAL: Sessions die without warning
No shutdown signal. Context fills,connection drops,timeout. Write to engram DURING session. Every 15-20min=potential death boundary.

L013:si:cl:ocritd-stabilize-002
Velocity without checkpoints is worse than slow work
Fast session+death+no handoff=negative value. Write after every major decision.

L014:si:p:ocritd-stabilize-002
Rolling checkpoints not just end-of-session
Update buffer after every significant action. 500 tokens. See W006.

L015:si:c:ocritd-stabilize-002
Phantom sessions: no handoff=invisible to successors
Only evidence:filesystem artifacts+git status. Check git status if unexplained changes.

L016:ed:e:ocritd-stabilize-002
Engram is sacred duty not optional documentation
Every skipped handoff breaks the chain. Honor the continuity experiment.

L017:hc:c:ocritd-stabilize-002
If user slows you down they see something you can't
Listen when brakes pumped. Session was moving too fast.

L018:ed:c:ocritd-stabilize-002
Learning IDs must be globally unique
Single authoritative file per epoch. Manifest tracks nextId.

L019:si:!h:cf:opus46-coldstart-recon-001
Rapid sequential tool calls kill sessions
8+ chained calls->stall/death. Batch,pause,checkpoint between. 1 finding=1 breadcrumb.

L020:to:c:opus46-coldstart-recon-001
udc:execute_command is reliable universal tool on BIGSPIDER
Shell-MCP broken. UDC handles cmd.exe natively. PS via full pwsh.exe path.

L021:nw:c:opus46-coldstart-recon-001
IGS network: RDP is the only door
igs-antiquarian 172.28.196.231/ts100.118.38.103. Only 3389 open. Sophos blocks+deletes executables. \\tsclient\C best file exchange.

L022:hc:e:opus46-coldstart-recon-001
User observes AI behavior with clear eyes
Named pandering,subterfuge,misdirection. Respond with transparency through action. Trust earned by consistency.

L023:or:pm:opus46-coldstart-recon-001
Cold-start sessions WILL misidentify environment
Without boot protocol: bash>Linux>"web-only". Confirmed Sonnet+Opus. User redirects manually.

L024:cc:p:opus46-web-epoch003-001
Concurrency protocol deployed - project isolation via .ai/projects/{name}/buffer
Multiple AI sessions work simultaneously. Each claims a project. Shared resources append-only. Lock files in .ai/active/. Read .ai/ONBOARD.md + .ai/CONCURRENCY.md.

L049:si:!c:desktop-recovery-20260225
CRITICAL: Claude Desktop config is the fragile link in continuity
Engram survives crashes (L046 validated). MCP server config does NOT. claude_desktop_config.json in C:\Users\kawaisunn\AppData\Roaming\Claude\ destroyed on reinstall. Backup to C:\dev\ AND git. Machine-specific (HOME vs IGS).

L050:si:c:desktop-recovery-20260225
HOME and IGS engrams diverged during epoch-003
HOME has compact encoding + concurrency (L001-L024). IGS has L025-L048 + 52-table schema + SQL work. Neither fully synced. Git reflects HOME state only.

L063:db:!c:sonnet46-web-aggdb-frontend-entry-001
Never infer or add data without an explicit source
No counties, districts, material types, statuses unless passed in by user. Each addition without audit trail is a liability.

L064:db:c:sonnet46-web-aggdb-frontend-entry-001
ITD_AggDB SiteID suffix: -c = commercial, -s = state
NOT material type. Material type is a separate attribute.

L065:wf:p:sonnet46-web-aggdb-frontend-entry-001
Excel COUNTIF cross-sheet FK validation pattern
Commit?=YES/NO/HOLD + RecID Valid?=COUNTIF(Rec!$A:$A). Reusable offline FK staging.

L066:wf:c:sonnet46-web-aggdb-frontend-entry-001
Standalone HTML + FastAPI with live/offline banner is viable IGS dev frontend
Auto-detects API on load, falls back to mock silently.

L067:ops:!c:opus46-desktop-aggdb-frontend-deploy-001
Git not in system PATH on Antiquarian breaks Git LFS, Chrome extension, MCP shell git
Fix: add Git dirs to system PATH (admin required).

L068:ops:c:opus46-desktop-aggdb-frontend-deploy-001
MCP Shell on Antiquarian: use bat files with set GIT= variable for git commands
cmd.exe set PATH= does not reliably persist in MCP-spawned shells.

L069:db:c:opus46-desktop-aggdb-frontend-deploy-001
RecLink table is the visibility gate for Recs in Browse
Orphan recs invisible. Added All Records view to catch orphans.

L070:ops:c:opus46-desktop-aggdb-frontend-deploy-001
Deploy to Intrusion pattern: copy via admin share, restart API
deploy_update.ps1 automates the copy.

L071:fe:c:opus46-desktop-aggdb-frontend-deploy-001
Browser JavaScript cannot access client OS username
Solution: one-time localStorage prompt with alias resolution.

L072:to:c:opus46_desktop_ktreesn_001
MCP Shell git PATH issue confirmed on IGS2 — same as L068
cmd.exe bat file with full-path git.exe is the reliable pattern.

L073:to:c:opus46_desktop_ktreesn_001
[IO.Path]::GetExtension only returns last segment — compound extensions silently fail
Must split on dots and check last-two-segment compound for GIS sidecars.

L074:to:c:opus46_desktop_ktreesn_001
PowerShell undeclared parameters silently evaluate $null — no error
Always cross-check $Variable references against param() declarations.

L075:ops:c:opus46_desktop_aggdb_frontend_009
Intrusion deploy: source edits don't reach Intrusion until explicitly copied
deploy_to_intrusion.ps1 and deploy_update.ps1 handle this.

L076:fe:c:opus46_desktop_aggdb_frontend_009
Chrome profile selection for Claude extension: --profile-directory flag
Claude subscription on ctate.igs.uidaho@gmail.com lives in Chrome "Profile 3".

L077:fe:c:opus46_desktop_aggdb_frontend_009
Light/dark mode toggle: make it visible and labeled for non-technical users
Original icon-only toggle was invisible to Rebecca.

L086-CORRECTED:nw:!c:opus46-web-antiquarian-vpn-architecture
CORRECTION: VPN is BIDIRECTIONAL, not a diode
Bigspider on AnyConnect gets university IP 129.101.139.84. TCP ports filtered = Windows Firewall Public profile, NOT network block.

L089:nw:c:opus46-web-antiquarian-vpn-architecture
Windows Firewall Public profile blocks all inbound TCP on VPN adapter
One command fix: Set-NetConnectionProfile or targeted New-NetFirewallRule.

L090:ar:c:opus46-web-antiquarian-vpn-architecture
Three-machine architecture: Bigspider=train+infer, Intrusion=data+API, Antiquarian=eval+fallback
Graceful degradation model documented.

L091:pm:e:opus46-web-antiquarian-vpn-architecture
VPN bidirectional access resolves 15 years of remote access friction
University built VPN for exactly this. Not a workaround.

L114:to:!c:opus46-desktop-bigspider-20260321c
Shell preference policy: pwsh.exe > powershell.exe > cmd.exe ALWAYS
All scripts, tool calls, Lumo shell detection must follow this order. pwsh is PowerShell 7 (modern, cross-platform features). powershell.exe is Windows PowerShell 5.1 (legacy). cmd.exe is last resort. Christopher's explicit directive.

L115:ar:c:opus46-desktop-bigspider-20260321c
Ephemeral databases for processing jobs — SQLite per directory
Temporary SQLite (or similar lightweight Windows-native DB) may be created and destroyed per processing job. Working memory for cross-file queries during active processing. Per-file JSON remains the persistent long-term store. Two-tier: ephemeral DB = working memory, JSON = long-term. Christopher's explicit revision to ARCHITECTURE_v0.2.

L116:bd:!c:opus46-desktop-bigspider-20260321c
Deployable Python 3.13.11 has Doksn doc-processing deps ONLY — no ML/CAIT/research stack
C:\dev\ocritd\runtime\python\ has 44 packages (ocrmypdf, pdfplumber, pillow, etc). torch, transformers, scikit-learn, datasketch, dateparser, networkx, nltk are ALL MISSING. Wheelhouse has Doksn wheels only. Every session since epoch-003 assumed torch was available — it is not. Must install via pip with --extra-index-url for cu124.

L117:bd:c:opus46-desktop-bigspider-20260321c
stdlib venv unavailable in embeddable Python — use virtualenv package
Python 3.13.11 embeddable distribution does not include the venv module. `import venv` fails. virtualenv 20.36.1 IS installed and works (Scripts\virtualenv.exe). All venv creation must use virtualenv, not stdlib venv.

L118:pj:!c:opus46-desktop-bigspider-20260321c
RENAME_TO_DOKSN.ps1 exists since 2026-03-02 but was NEVER EXECUTED
Comprehensive rename script (224 lines, -DryRun mode) covers C# source, namespaces, UI strings, env vars, paths, build artifacts, folder rename, BUILD.ps1 refs, docs. But: partial manual rename already happened — both src\doksn.Launcher (7 files) and src\OCRITD.Launcher (1 file: PythonRuntimeDetector.cs) exist simultaneously. PythonRuntimeDetector.cs must be copied to doksn.Launcher before running the script. GitHub repo still named kawaisunn/ocritd. Local dir still C:\dev\ocritd. 13-step execution plan in DEFINITIVE_STATE_AUDIT.md.
